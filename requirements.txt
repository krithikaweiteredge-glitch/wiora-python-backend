# Wiora Python backend (FastAPI) — V1 core per the technology blueprint.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
python-jose[cryptography]==3.3.0
httpx==0.28.1
openai==1.59.6            # used for Groq (OpenAI-compatible API)
redis==5.2.1              # caching, rate limiting, agent state (Celery queue later)
python-dotenv==1.0.1
pymupdf==1.25.1
python-docx==1.1.2
openpyxl==3.1.5           # XLSX text extraction for the document pipeline
fastembed>=0.4,<0.6
alembic==1.14.0           # database migrations (replaces create_all going forward)
sentry-sdk[fastapi]==2.19.2  # optional error tracking (no-op without SENTRY_DSN)
celery==5.4.0             # workflow engine: scheduled reminders / briefing push
# For FCM push, also: pip install firebase-admin (kept optional; see workflow/push).
